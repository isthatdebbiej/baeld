# `baeld-agent`

Python adapter for a Chromium session started by `baeld run`. `connect_browser_use` attaches Browser Use to the owned browser, while `BaeldPhaseModel` wraps `BaseChatModel.ainvoke` to expose exact inference waits.
