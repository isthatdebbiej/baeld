# `@baeld/agent`

JavaScript and TypeScript adapter for a Chromium session started by `baeld run`. It provides acknowledged phases, Playwright attachment, Stagehand supervision, resource filtering, and a model-wait helper. Stagehand's combined public calls do not expose an internal suspend-safe inference boundary; see the repository integration guide.
