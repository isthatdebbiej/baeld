# Results

Benchmark commands create immutable run directories here. Raw result data is not committed during development. The release dataset will be attached to the corresponding GitHub release and selected reproducible runs may be committed under a versioned subdirectory.

