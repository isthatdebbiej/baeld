# Results

Benchmark commands create immutable run directories here. Raw result data is
not committed during development. Release datasets are attached to their
corresponding GitHub release.

Schema 4 terminal events include `concurrency` and `block_id`; analyzers use
both to prevent unlike cells from being pooled. Schema 1–3 files remain
readable, but only their concurrency-one rows can use legacy occurrence-order
pairing.
